# CLAUDE.md

__PROJECT__ は Cradle（Lean 仕様駆動開発ハーネス）で育てるプロダクト。
規約は `.claude/rules/`（Cradle が配る）にあり、ここにはこのプロジェクト固有の事実だけを書く。

## 現在地

`cradle status`（`node .claude/skills/cradle/scripts/cradle.mjs status`）が正。

## 開発の順序

`.claude/rules/cradle-core.md` が正（ここに写さない）。

## このプロジェクト固有の事実

- ドメイン: （一言で）
- 構成: `lean/`（実行可能仕様）・`documents/`（探索・設計の正本）・`backend/`・`frontend/`・`e2e/`・`infra/`
- 承認済みの固有の決めごと（ai-notes から昇格したもの）: `documents/decisions.md`

# インフラ設計

パイプライン（探索 → **インフラ設計** → Lean 化 → … → バックエンド → **インフラ実装** → E2E → **本番投入**）の独立フェーズの成果物。
ここが test / local / 本番のインフラの**正本**で、実装（`infra/`）はこれに従う。

| ドキュメント | 内容 |
|---|---|
| [01_model-to-infra.md](01_model-to-infra.md) | Lean の実行可能仕様 → 非機能要件の導出。**作らないもの**の一覧を含む |
| 02_local.md | ローカルの構成 |
| 03_production.md | 本番の構成 |
| 04_operations.md | 運用・デプロイ手順・障害時 |

## 環境

| 環境 | 実行の場 | DB | 認証 | 用途 |
|---|---|---|---|---|
| `test` | | | 無し（名義はテストから差し込む） | 外部依存なしで完結させる |
| `local` | | | モック発行者 | 開発・本番同等の確認 |
| `prod` | | | | 本番 |

## 決定一覧

根拠の「モデル」は `lean/` の実行可能仕様、「探索」は `documents/ddd/` の正式ドキュメント。

| ID | 決定 | 根拠 |
|---|---|---|

## 前提（documents に裏付けが無く、インフラ設計のために置いた仮定）

**これらはドメインの事実ではない。** サイジングの根拠としてのみ使い、モデルに書き戻さない。

| ID | 前提 | 効いている決定 |
|---|---|---|

## 未決（ユーザーの判断が要る）

状況が空か `open` / `未決` で始まる行が未決。決着したら `決着。INFRA-D-nnn`（写した先）、決めないと決めたら `却下。理由`。

| ID | 問い | 暫定 | 状況 |
|---|---|---|---|

## 投入

設計どおりに実装が動いた事実。`未` の環境を本番と呼ばない。

| 環境 | 投入 | 記録 |
|---|---|---|
| `local` | 未 | |
| `prod` | 未 | |

## 実装後の見直し

実装が入って前提が変わったら、直した決定（INFRA-D）と変わった事実だけを書く。

# モデル → インフラ要件の導出

`lean/` の実行可能仕様と `documents/ddd/` の正式ドキュメントだけを入力に、
インフラが満たすべきことと**作ってはいけないもの**を出す。
`ux-review.md` / `model-review.md` の open 項目は根拠に使わない。

## 1. 入口

## 2. 状態と直列化点（モデルの横断不変条件がどこで守られるか）

## 3. 時計と暦（`today` をどこから取るか）

## 4. 名義（誰として操作しているかをどこで確定するか）

## 5. 同一性の表現（泉の具体化 — 連番か UUID か。NFR を根拠に決め、モデルへ反映する）

## 6. 作らないもの（モデルに裏付けの無いインフラの一覧）

| 作らないもの | 根拠 |
|---|---|

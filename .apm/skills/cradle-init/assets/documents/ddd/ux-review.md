# UX レビュー（利用者視点の指摘・提案）

`ddd-ux-reviewer` が、この場にいない利用者の視点を代弁して起票する**未検証の仮説**。
正式ドキュメントではない — ここの open 項目はドメインの事実の裏付けにならない。

open 項目は次回の探索セッションで `ddd-domain-explorer` がエキスパートに検証し、
`反映済`（正式ドキュメントへ反映）または `却下`（理由を必ず記録）に更新する。

| ID | 種別 | 指摘 / 提案 | 想定利用者 | 状態 | 結果 |
|---|---|---|---|---|---|

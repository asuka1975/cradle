// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.lobby.domain.entity

import dev.cradle.lobby.VisitFixture
import dev.cradle.lobby.VisitorNameFixture
import dev.cradle.lobby.domain.valueobject.VisitId
import dev.cradle.lobby.domain.valueobject.VisitPhase
import dev.cradle.lobby.domain.valueobject.VisitorName
import dev.cradle.lobby.runtime.EmployeeId
import dev.cradle.lobby.toFixture
import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

/**
 * Lean: @[contract] 定理群から演繹された `Visit` の単体テスト。
 * ふるまいは Entity interface のメソッド — 入力は実体化フック
 * (fixture → 実装の Entity)で作り、返り値は観測(toFixture)で比較する。
 */
abstract class VisitEntityContractTest {
	/** fixture の実体化(観測が一致する実装の Entity を返す)。 */
	protected abstract fun visit(fixture: VisitFixture): Visit

	/** 受け付けた直後は受付中。 */
	@Test
	fun `isExpected は定理 book_expected を再現する(1)`() {
		assertEquals(true,
			visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s6", visitor = VisitorNameFixture(text = "s9"), phase = VisitPhase.Expected)).isExpected())
	}

	/** 受け付けた直後は受付中。 */
	@Test
	fun `isExpected は定理 book_expected を再現する(2)`() {
		assertEquals(true,
			visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s6", visitor = VisitorNameFixture(text = "s10"), phase = VisitPhase.Expected)).isExpected())
	}

	/** 受け付けた直後は受付中。 */
	@Test
	fun `isExpected は定理 book_expected を再現する(3)`() {
		assertEquals(true,
			visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s6", visitor = VisitorNameFixture(text = "s11"), phase = VisitPhase.Expected)).isExpected())
	}

	/** 受け付けた直後は受付中。 */
	@Test
	fun `isExpected は定理 book_expected を再現する(4)`() {
		assertEquals(true,
			visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s6", visitor = VisitorNameFixture(text = "s12"), phase = VisitPhase.Expected)).isExpected())
	}

	/** 退出しても同一性は変わらない。 */
	@Test
	fun `leave は定理 leave_id を再現する(1)`() {
		assertEquals(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s0", visitor = VisitorNameFixture(text = "s1"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s0", visitor = VisitorNameFixture(text = "s1"), phase = VisitPhase.Expected)).leave().toFixture())
	}

	/** 退出しても同一性は変わらない。 */
	@Test
	fun `leave は定理 leave_id を再現する(2)`() {
		assertEquals(VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s1", visitor = VisitorNameFixture(text = "s2"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s1", visitor = VisitorNameFixture(text = "s2"), phase = VisitPhase.Left)).leave().toFixture())
	}

	/** 退出しても同一性は変わらない。 */
	@Test
	fun `leave は定理 leave_id を再現する(3)`() {
		assertEquals(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s2", visitor = VisitorNameFixture(text = "s3"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s2", visitor = VisitorNameFixture(text = "s3"), phase = VisitPhase.Left)).leave().toFixture())
	}

	/** 退出しても同一性は変わらない。 */
	@Test
	fun `leave は定理 leave_id を再現する(4)`() {
		assertEquals(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 7L), hostName = "s3", visitor = VisitorNameFixture(text = "s4"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 7L), hostName = "s3", visitor = VisitorNameFixture(text = "s4"), phase = VisitPhase.Left)).leave().toFixture())
	}

	/** 退出は冪等。 */
	@Test
	fun `leave は定理 leave_idem を再現する(1)`() {
		assertEquals(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s0", visitor = VisitorNameFixture(text = "s1"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s0", visitor = VisitorNameFixture(text = "s1"), phase = VisitPhase.Left)).leave().toFixture())
	}

	/** 退出は冪等。 */
	@Test
	fun `leave は定理 leave_idem を再現する(2)`() {
		assertEquals(VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s1", visitor = VisitorNameFixture(text = "s2"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s1", visitor = VisitorNameFixture(text = "s2"), phase = VisitPhase.Left)).leave().toFixture())
	}

	/** 退出は冪等。 */
	@Test
	fun `leave は定理 leave_idem を再現する(3)`() {
		assertEquals(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s2", visitor = VisitorNameFixture(text = "s3"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s2", visitor = VisitorNameFixture(text = "s3"), phase = VisitPhase.Left)).leave().toFixture())
	}

	/** 退出は冪等。 */
	@Test
	fun `leave は定理 leave_idem を再現する(4)`() {
		assertEquals(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 7L), hostName = "s3", visitor = VisitorNameFixture(text = "s4"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 7L), hostName = "s3", visitor = VisitorNameFixture(text = "s4"), phase = VisitPhase.Left)).leave().toFixture())
	}

	/** 退出したら退出済み。 */
	@Test
	fun `leave は定理 leave_phase を再現する(1)`() {
		assertEquals(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s0", visitor = VisitorNameFixture(text = "s1"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s0", visitor = VisitorNameFixture(text = "s1"), phase = VisitPhase.Expected)).leave().toFixture())
	}

	/** 退出したら退出済み。 */
	@Test
	fun `leave は定理 leave_phase を再現する(2)`() {
		assertEquals(VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s1", visitor = VisitorNameFixture(text = "s2"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s1", visitor = VisitorNameFixture(text = "s2"), phase = VisitPhase.Left)).leave().toFixture())
	}

	/** 退出したら退出済み。 */
	@Test
	fun `leave は定理 leave_phase を再現する(3)`() {
		assertEquals(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s2", visitor = VisitorNameFixture(text = "s3"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s2", visitor = VisitorNameFixture(text = "s3"), phase = VisitPhase.Left)).leave().toFixture())
	}

	/** 退出したら退出済み。 */
	@Test
	fun `leave は定理 leave_phase を再現する(4)`() {
		assertEquals(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 7L), hostName = "s3", visitor = VisitorNameFixture(text = "s4"), phase = VisitPhase.Left),
			visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 7L), hostName = "s3", visitor = VisitorNameFixture(text = "s4"), phase = VisitPhase.Left)).leave().toFixture())
	}
}

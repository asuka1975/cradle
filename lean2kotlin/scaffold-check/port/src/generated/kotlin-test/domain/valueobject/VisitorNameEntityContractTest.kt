// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.lobby.domain.valueobject

import dev.cradle.lobby.VisitorNameFixture
import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

/**
 * Lean: @[contract] 定理群から演繹された `VisitorName` の単体テスト。
 * ふるまいは Entity interface のメソッド — 入力は実体化フック
 * (fixture → 実装の Entity)で作り、返り値は観測(toFixture)で比較する。
 */
abstract class VisitorNameEntityContractTest {
	/** fixture の実体化(観測が一致する実装の Entity を返す)。 */
	protected abstract fun visitorName(fixture: VisitorNameFixture): VisitorName

	@Test
	fun `valid は定理 valid_iff を再現する(1)`() {
		assertEquals(true,
			visitorName(VisitorNameFixture(text = "s0")).valid())
	}

	@Test
	fun `valid は定理 valid_iff を再現する(2)`() {
		assertEquals(true,
			visitorName(VisitorNameFixture(text = "s1")).valid())
	}

	@Test
	fun `valid は定理 valid_iff を再現する(3)`() {
		assertEquals(true,
			visitorName(VisitorNameFixture(text = "s2")).valid())
	}

	@Test
	fun `valid は定理 valid_iff を再現する(4)`() {
		assertEquals(true,
			visitorName(VisitorNameFixture(text = "s3")).valid())
	}
}

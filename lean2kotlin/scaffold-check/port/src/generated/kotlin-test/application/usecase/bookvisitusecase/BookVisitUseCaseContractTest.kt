// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.lobby.application.usecase.bookvisitusecase

import dev.cradle.lobby.DomainResult
import dev.cradle.lobby.VisitFixture
import dev.cradle.lobby.VisitorNameFixture
import dev.cradle.lobby.application.ActorContext
import dev.cradle.lobby.application.VisitIdGenerator
import dev.cradle.lobby.application.port.organizationdirectory.OrganizationDirectory
import dev.cradle.lobby.application.port.organizationdirectory.OrganizationDirectoryFindMemberMember
import dev.cradle.lobby.application.port.organizationdirectory.OrganizationDirectoryFindMemberOutcome
import dev.cradle.lobby.application.port.organizationdirectory.OrganizationDirectoryFindMemberRequest
import dev.cradle.lobby.application.port.organizationdirectory.OrganizationDirectoryMock
import dev.cradle.lobby.domain.DomainError
import dev.cradle.lobby.domain.entity.Visit
import dev.cradle.lobby.domain.repository.VisitRepository
import dev.cradle.lobby.domain.valueobject.VisitId
import dev.cradle.lobby.domain.valueobject.VisitPhase
import dev.cradle.lobby.domain.valueobject.VisitorName
import dev.cradle.lobby.runtime.EmployeeId
import dev.cradle.lobby.runtime.UserId
import dev.cradle.lobby.toFixture
import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

/**
 * Lean: @[contract] 契約定理から演繹された BookVisitUseCase のパラメトリックテスト。
 * 契約は**状態遷移**を本物の Repository で検査する:
 * 作用前を add で播種 → execute / validate → 作用後を集約単位の観測
 * (findAll — 並びは保存順)で突き合わせる。repository() は
 * **呼び出しごとに空のリポジトリ**を返すこと。採番は注入された
 * IdGenerator から消費順どおりに採番すること(泉の契約)。
 */
abstract class BookVisitUseCaseContractTest {
	protected abstract fun visitRepository(): VisitRepository
	/** fixture の実体化(観測が一致する実装の値を返す)。 */
	protected abstract fun visit(fixture: VisitFixture): Visit
	/** fixture の実体化(観測が一致する実装の値を返す)。 */
	protected abstract fun visitorName(fixture: VisitorNameFixture): VisitorName
	/** 実装を、観測用リポジトリと泉ごとの採番ポートを配線して返す。主体は固定の actor を配線する(主体ポート。主体依存のふるまい = 認可分岐はこの注入で検証される)。外部能力の Port は生成モック(OrganizationDirectoryMock)を配線する(要求の値と呼び出し回数はモックが検査する)。 */
	protected abstract fun useCase(visitRepository: VisitRepository, organizationDirectory: OrganizationDirectory, visitIdGenerator: VisitIdGenerator, actor: ActorContext): BookVisitUseCase

	/** 連番採番 — 状態(visitIds.next)を種とする決定的実装。 */
	protected class SequentialVisitIdGenerator(var next: Long) : VisitIdGenerator {
		override fun nextId(): VisitId = VisitId(next++)
	}

	/** ディレクトリが答えなければ受け付けない（いないのとは別の拒否）。 */
	@Test
	fun `execute は定理 execute_directory_unavailable を再現する(1)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Unavailable)))
		val visitIdGenerator = SequentialVisitIdGenerator(501L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.DirectoryUnavailable),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s6")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(501L, visitIdGenerator.next)
	}

	/** ディレクトリが答えなければ受け付けない（いないのとは別の拒否）。 */
	@Test
	fun `execute は定理 execute_directory_unavailable を再現する(2)`() {
		val visitRepository = visitRepository()
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s11", visitor = VisitorNameFixture(text = "s12"), phase = VisitPhase.Left)))
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Unavailable)))
		val visitIdGenerator = SequentialVisitIdGenerator(502L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.DirectoryUnavailable),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s6")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s11", visitor = VisitorNameFixture(text = "s12"), phase = VisitPhase.Left)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(502L, visitIdGenerator.next)
	}

	/** ディレクトリが答えなければ受け付けない（いないのとは別の拒否）。 */
	@Test
	fun `execute は定理 execute_directory_unavailable を再現する(3)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Unavailable)))
		val visitIdGenerator = SequentialVisitIdGenerator(506L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.DirectoryUnavailable),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s6")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(506L, visitIdGenerator.next)
	}

	/** ディレクトリが答えなければ受け付けない（いないのとは別の拒否）。 */
	@Test
	fun `execute は定理 execute_directory_unavailable を再現する(4)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 5L)), outcome = OrganizationDirectoryFindMemberOutcome.Unavailable)))
		val visitIdGenerator = SequentialVisitIdGenerator(501L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.DirectoryUnavailable),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 5L), visitor = visitorName(VisitorNameFixture(text = "s7")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(501L, visitIdGenerator.next)
	}

	/** 来訪者の名前が空なら、どの観測でも受け付けない（外部は呼ばれない）。 */
	@Test
	fun `execute は定理 execute_empty_visitor を再現する(1)`() {
		val visitRepository = visitRepository()
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s12", visitor = VisitorNameFixture(text = "s13"), phase = VisitPhase.Expected)))
		val organizationDirectory = OrganizationDirectoryMock(emptyList())
		val visitIdGenerator = SequentialVisitIdGenerator(500L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.EmptyVisitor),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 9L), visitor = visitorName(VisitorNameFixture(text = "")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s12", visitor = VisitorNameFixture(text = "s13"), phase = VisitPhase.Expected)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(500L, visitIdGenerator.next)
	}

	/** 来訪者の名前が空なら、どの観測でも受け付けない（外部は呼ばれない）。 */
	@Test
	fun `execute は定理 execute_empty_visitor を再現する(2)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(emptyList())
		val visitIdGenerator = SequentialVisitIdGenerator(501L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.EmptyVisitor),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 9L), visitor = visitorName(VisitorNameFixture(text = "")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(501L, visitIdGenerator.next)
	}

	/** 来訪者の名前が空なら、どの観測でも受け付けない（外部は呼ばれない）。 */
	@Test
	fun `execute は定理 execute_empty_visitor を再現する(3)`() {
		val visitRepository = visitRepository()
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s14", visitor = VisitorNameFixture(text = "s15"), phase = VisitPhase.Left)))
		val organizationDirectory = OrganizationDirectoryMock(emptyList())
		val visitIdGenerator = SequentialVisitIdGenerator(502L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.EmptyVisitor),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 9L), visitor = visitorName(VisitorNameFixture(text = "")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s14", visitor = VisitorNameFixture(text = "s15"), phase = VisitPhase.Left)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(502L, visitIdGenerator.next)
	}

	/** 来訪者の名前が空なら、どの観測でも受け付けない（外部は呼ばれない）。 */
	@Test
	fun `execute は定理 execute_empty_visitor を再現する(4)`() {
		val visitRepository = visitRepository()
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s15", visitor = VisitorNameFixture(text = "s16"), phase = VisitPhase.Expected)))
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s17", visitor = VisitorNameFixture(text = "s18"), phase = VisitPhase.Left)))
		val organizationDirectory = OrganizationDirectoryMock(emptyList())
		val visitIdGenerator = SequentialVisitIdGenerator(503L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.EmptyVisitor),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 9L), visitor = visitorName(VisitorNameFixture(text = "")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s15", visitor = VisitorNameFixture(text = "s16"), phase = VisitPhase.Expected), VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s17", visitor = VisitorNameFixture(text = "s18"), phase = VisitPhase.Left)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(503L, visitIdGenerator.next)
	}

	/** 在籍していない受入担当者では受け付けない。 */
	@Test
	fun `execute は定理 execute_host_inactive を再現する(1)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Found(member = OrganizationDirectoryFindMemberMember(name = "s6", active = false)))))
		val visitIdGenerator = SequentialVisitIdGenerator(501L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.HostInactive),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s9")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(501L, visitIdGenerator.next)
	}

	/** 在籍していない受入担当者では受け付けない。 */
	@Test
	fun `execute は定理 execute_host_inactive を再現する(2)`() {
		val visitRepository = visitRepository()
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s14", visitor = VisitorNameFixture(text = "s15"), phase = VisitPhase.Left)))
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Found(member = OrganizationDirectoryFindMemberMember(name = "s6", active = false)))))
		val visitIdGenerator = SequentialVisitIdGenerator(502L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.HostInactive),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s9")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s14", visitor = VisitorNameFixture(text = "s15"), phase = VisitPhase.Left)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(502L, visitIdGenerator.next)
	}

	/** 在籍していない受入担当者では受け付けない。 */
	@Test
	fun `execute は定理 execute_host_inactive を再現する(3)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Found(member = OrganizationDirectoryFindMemberMember(name = "s6", active = false)))))
		val visitIdGenerator = SequentialVisitIdGenerator(506L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.HostInactive),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s9")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(506L, visitIdGenerator.next)
	}

	/** 在籍していない受入担当者では受け付けない。 */
	@Test
	fun `execute は定理 execute_host_inactive を再現する(4)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 5L)), outcome = OrganizationDirectoryFindMemberOutcome.Found(member = OrganizationDirectoryFindMemberMember(name = "s6", active = false)))))
		val visitIdGenerator = SequentialVisitIdGenerator(501L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.HostInactive),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 5L), visitor = visitorName(VisitorNameFixture(text = "s10")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(501L, visitIdGenerator.next)
	}

	/** 受入担当者がいなければ受け付けない。 */
	@Test
	fun `execute は定理 execute_host_missing を再現する(1)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Missing)))
		val visitIdGenerator = SequentialVisitIdGenerator(501L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.HostMissing),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s6")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(501L, visitIdGenerator.next)
	}

	/** 受入担当者がいなければ受け付けない。 */
	@Test
	fun `execute は定理 execute_host_missing を再現する(2)`() {
		val visitRepository = visitRepository()
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s11", visitor = VisitorNameFixture(text = "s12"), phase = VisitPhase.Left)))
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Missing)))
		val visitIdGenerator = SequentialVisitIdGenerator(502L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.HostMissing),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s6")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s11", visitor = VisitorNameFixture(text = "s12"), phase = VisitPhase.Left)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(502L, visitIdGenerator.next)
	}

	/** 受入担当者がいなければ受け付けない。 */
	@Test
	fun `execute は定理 execute_host_missing を再現する(3)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Missing)))
		val visitIdGenerator = SequentialVisitIdGenerator(506L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.HostMissing),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s6")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(506L, visitIdGenerator.next)
	}

	/** 受入担当者がいなければ受け付けない。 */
	@Test
	fun `execute は定理 execute_host_missing を再現する(4)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 5L)), outcome = OrganizationDirectoryFindMemberOutcome.Missing)))
		val visitIdGenerator = SequentialVisitIdGenerator(501L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.HostMissing),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 5L), visitor = visitorName(VisitorNameFixture(text = "s7")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(501L, visitIdGenerator.next)
	}

	/** 受付中の来訪があれば、どの観測でも受け付けない。 */
	@Test
	fun `execute は定理 execute_occupied を再現する(1)`() {
		val visitRepository = visitRepository()
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s12", visitor = VisitorNameFixture(text = "s13"), phase = VisitPhase.Expected)))
		val organizationDirectory = OrganizationDirectoryMock(emptyList())
		val visitIdGenerator = SequentialVisitIdGenerator(500L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.LobbyOccupied),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s9")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s12", visitor = VisitorNameFixture(text = "s13"), phase = VisitPhase.Expected)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(500L, visitIdGenerator.next)
	}

	/** 受付中の来訪があれば、どの観測でも受け付けない。 */
	@Test
	fun `execute は定理 execute_occupied を再現する(2)`() {
		val visitRepository = visitRepository()
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s15", visitor = VisitorNameFixture(text = "s16"), phase = VisitPhase.Expected)))
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s17", visitor = VisitorNameFixture(text = "s18"), phase = VisitPhase.Left)))
		val organizationDirectory = OrganizationDirectoryMock(emptyList())
		val visitIdGenerator = SequentialVisitIdGenerator(503L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.LobbyOccupied),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s9")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s15", visitor = VisitorNameFixture(text = "s16"), phase = VisitPhase.Expected), VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s17", visitor = VisitorNameFixture(text = "s18"), phase = VisitPhase.Left)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(503L, visitIdGenerator.next)
	}

	/** 受付中の来訪があれば、どの観測でも受け付けない。 */
	@Test
	fun `execute は定理 execute_occupied を再現する(3)`() {
		val visitRepository = visitRepository()
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s16", visitor = VisitorNameFixture(text = "s17"), phase = VisitPhase.Expected)))
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s18", visitor = VisitorNameFixture(text = "s19"), phase = VisitPhase.Left)))
		val organizationDirectory = OrganizationDirectoryMock(emptyList())
		val visitIdGenerator = SequentialVisitIdGenerator(504L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.LobbyOccupied),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s9")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s16", visitor = VisitorNameFixture(text = "s17"), phase = VisitPhase.Expected), VisitFixture(id = VisitId(id = 92L), host = EmployeeId(id = 5L), hostName = "s18", visitor = VisitorNameFixture(text = "s19"), phase = VisitPhase.Left)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(504L, visitIdGenerator.next)
	}

	/** 受付中の来訪があれば、どの観測でも受け付けない。 */
	@Test
	fun `execute は定理 execute_occupied を再現する(4)`() {
		val visitRepository = visitRepository()
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s17", visitor = VisitorNameFixture(text = "s18"), phase = VisitPhase.Expected)))
		val organizationDirectory = OrganizationDirectoryMock(emptyList())
		val visitIdGenerator = SequentialVisitIdGenerator(505L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Err(DomainError.LobbyOccupied),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s9")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 91L), host = EmployeeId(id = 4L), hostName = "s17", visitor = VisitorNameFixture(text = "s18"), phase = VisitPhase.Expected)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(505L, visitIdGenerator.next)
	}

	/** 見つかって在籍なら受け付ける。 */
	@Test
	fun `execute は定理 execute_ok を再現する(1)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Found(member = OrganizationDirectoryFindMemberMember(name = "s7", active = true)))))
		val visitIdGenerator = SequentialVisitIdGenerator(501L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Ok(Unit),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s9")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 501L), host = EmployeeId(id = 4L), hostName = "s7", visitor = VisitorNameFixture(text = "s9"), phase = VisitPhase.Expected)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(502L, visitIdGenerator.next)
	}

	/** 見つかって在籍なら受け付ける。 */
	@Test
	fun `execute は定理 execute_ok を再現する(2)`() {
		val visitRepository = visitRepository()
		visitRepository.add(visit(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s14", visitor = VisitorNameFixture(text = "s15"), phase = VisitPhase.Left)))
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Found(member = OrganizationDirectoryFindMemberMember(name = "s7", active = true)))))
		val visitIdGenerator = SequentialVisitIdGenerator(502L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Ok(Unit),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s9")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 93L), host = EmployeeId(id = 6L), hostName = "s14", visitor = VisitorNameFixture(text = "s15"), phase = VisitPhase.Left), VisitFixture(id = VisitId(id = 502L), host = EmployeeId(id = 4L), hostName = "s7", visitor = VisitorNameFixture(text = "s9"), phase = VisitPhase.Expected)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(503L, visitIdGenerator.next)
	}

	/** 見つかって在籍なら受け付ける。 */
	@Test
	fun `execute は定理 execute_ok を再現する(3)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 4L)), outcome = OrganizationDirectoryFindMemberOutcome.Found(member = OrganizationDirectoryFindMemberMember(name = "s7", active = true)))))
		val visitIdGenerator = SequentialVisitIdGenerator(506L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Ok(Unit),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 4L), visitor = visitorName(VisitorNameFixture(text = "s9")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 506L), host = EmployeeId(id = 4L), hostName = "s7", visitor = VisitorNameFixture(text = "s9"), phase = VisitPhase.Expected)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(507L, visitIdGenerator.next)
	}

	/** 見つかって在籍なら受け付ける。 */
	@Test
	fun `execute は定理 execute_ok を再現する(4)`() {
		val visitRepository = visitRepository()
		val organizationDirectory = OrganizationDirectoryMock(listOf(OrganizationDirectoryMock.FindMember(request = OrganizationDirectoryFindMemberRequest(employee = EmployeeId(id = 5L)), outcome = OrganizationDirectoryFindMemberOutcome.Found(member = OrganizationDirectoryFindMemberMember(name = "s7", active = true)))))
		val visitIdGenerator = SequentialVisitIdGenerator(501L)
		val useCase = useCase(visitRepository, organizationDirectory, visitIdGenerator, ActorContext(user = UserId(id = 4L)))
		assertEquals(DomainResult.Ok(Unit),
			runCatching { useCase.execute(c = BookVisitCommand(host = EmployeeId(id = 5L), visitor = visitorName(VisitorNameFixture(text = "s10")))) }.also { organizationDirectory.assertComplete() }.getOrThrow())
		assertEquals(listOf<VisitFixture>(VisitFixture(id = VisitId(id = 501L), host = EmployeeId(id = 5L), hostName = "s7", visitor = VisitorNameFixture(text = "s10"), phase = VisitPhase.Expected)),
			visitRepository.findAll().map { it.toFixture() })
		assertEquals(502L, visitIdGenerator.next)
	}
}

// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.lobby.application.port.organizationdirectory

import dev.cradle.lobby.PortHarnessFailure

/**
 * Lean: Port `Lobby.Application.Port.OrganizationDirectory` の契約テスト用モック。
 * 期待するやり取り(Lean が評価した要求と定理の観測)を順に積み、実装が同じ要求で呼べばその観測を返す。
 * 違う要求・積んでいない呼び出し・別の操作は記録して PortHarnessFailure を投げる(業務の拒否とは別)。
 * 被検査コードが例外を捕捉しても記録は残り、assertComplete が失敗にする。
 * 障害契約テストは消費位置を検査する: 外部を呼ぶ前の中断は assertUntouched、応答を得た後の中断は assertComplete。
 */
class OrganizationDirectoryMock(expected: List<Expectation>) : OrganizationDirectory {
	/** 期待するやり取り(操作ごとに 1 種)。 */
	sealed interface Expectation
	data class FindMember(val request: OrganizationDirectoryFindMemberRequest, val outcome: OrganizationDirectoryFindMemberOutcome) : Expectation

	private val queue = ArrayDeque(expected)
	private val expectedCount = expected.size
	private val failures = mutableListOf<String>()

	private fun fail(message: String): Nothing {
		failures += message
		throw PortHarnessFailure("OrganizationDirectory: $message")
	}

	override fun findMember(request: OrganizationDirectoryFindMemberRequest): OrganizationDirectoryFindMemberOutcome {
		val next = queue.removeFirstOrNull() ?: fail("findMember: 積んでいない呼び出し($request)")
		if (next !is FindMember) fail("findMember: 期待していた操作は $next")
		if (next.request != request) fail("findMember: 要求が違う — 期待 ${next.request} / 実際 $request")
		return next.outcome
	}

	/** 記録された失敗(要求不一致・積んでいない呼び出し)が無いこと。 */
	fun assertNoFailure() {
		if (failures.isNotEmpty()) throw AssertionError("OrganizationDirectory: " + failures.joinToString("\n"))
	}

	/** 失敗が無く、積んだやり取りが全部消費されたこと(呼ばれなかった要求も失敗)。 */
	fun assertComplete() {
		assertNoFailure()
		if (queue.isNotEmpty()) throw AssertionError("OrganizationDirectory: 呼ばれなかったやり取りが残っている: $queue")
	}

	/** 失敗が無く、まだ何も呼ばれていないこと(外部を呼ぶ前の中断の検査)。 */
	fun assertUntouched() {
		assertNoFailure()
		if (queue.size != expectedCount) throw AssertionError("OrganizationDirectory: 外部を呼ぶ前に中断するはずの位置で呼ばれている(消費 ${expectedCount - queue.size} 件)")
	}
}

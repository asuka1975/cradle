// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.lobby.application.port.organizationdirectory

import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Assertions.assertTrue
import org.junit.jupiter.api.Test

/**
 * Lean: Port `Lobby.Application.Port.OrganizationDirectory` の Adapter(外部の呼び方の実装)の適合テスト。
 * 観測(Outcome)の各構成子を Adapter が一度は産めることを検査する — 全応答への写像の正しさは主張しない
 * (写像の中身は手書きの Adapter 検査の持ち物)。stub / sandbox 相手に配線した Adapter を adapter() で返し、
 * 各 arrange フックで stub をその観測を返す状態にしてから要求を返す。通常の build には入らない(adapterContractTest タスク)。
 */
abstract class OrganizationDirectoryAdapterContractTest {
	/** 検査対象の Adapter(stub / sandbox 相手に配線したもの)。 */
	protected abstract fun adapter(): OrganizationDirectory
	/** 観測 found を返させる要求。stub をその状態にしてから返す。 */
	protected abstract fun arrangeFindMemberFound(): OrganizationDirectoryFindMemberRequest
	/** 観測 missing を返させる要求。stub をその状態にしてから返す。 */
	protected abstract fun arrangeFindMemberMissing(): OrganizationDirectoryFindMemberRequest
	/** 観測 unavailable を返させる要求。stub をその状態にしてから返す。 */
	protected abstract fun arrangeFindMemberUnavailable(): OrganizationDirectoryFindMemberRequest

	@Test
	fun `findMember は found を産める`() {
		val outcome = adapter().findMember(arrangeFindMemberFound())
		assertTrue(outcome is OrganizationDirectoryFindMemberOutcome.Found, "$outcome")
	}

	@Test
	fun `findMember は missing を産める`() {
		val outcome = adapter().findMember(arrangeFindMemberMissing())
		assertTrue(outcome is OrganizationDirectoryFindMemberOutcome.Missing, "$outcome")
	}

	@Test
	fun `findMember は unavailable を産める`() {
		val outcome = adapter().findMember(arrangeFindMemberUnavailable())
		assertTrue(outcome is OrganizationDirectoryFindMemberOutcome.Unavailable, "$outcome")
	}
}

// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.lobby.application.port.organizationdirectory

/**
 * Lean: Port `Lobby.Application.Port.OrganizationDirectory`(外部能力 — 自システムが必要とする能力の要求と観測)。
 * 操作ごとに要求(`Request`)を渡して観測(`Outcome`)を受ける。観測の構成子は外部で起き得る
 * ことの全部(見つからない・答えない等)で、Adapter は外部のあらゆる応答をどれかの構成子に写す。
 * 実装(Adapter)は AI が `infrastructure/<port>/` に別ファイルで書く。default 禁止。
 * 契約テストは生成モック(`OrganizationDirectoryMock`)を注入し、要求の値と呼び出し回数を検査する。
 */
interface OrganizationDirectory {
	/** 要求: 誰を引くか。 */
	fun findMember(request: OrganizationDirectoryFindMemberRequest): OrganizationDirectoryFindMemberOutcome
}

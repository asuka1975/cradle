// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.lobby.application

import dev.cradle.lobby.domain.valueobject.VisitId

/**
 * Lean: `Lobby.Application.VisitIdGeneratorState` — 泉(同一性の供給)の観測モデルに対応する
 * IdGenerator ポート(命名規約 `<X>State ↔ <X>`)。
 * 来訪の採番（VisitIdGenerator ポート）のカウンタ具体化の論理状態: 次に配る値。
 *
 * 供給値の型はモデルの具体化の写し。freshness(供給値の相異・
 * 既使用との不交和)は実装の義務。契約テストは決定的な供給(Sequential 相当)を
 * 注入する — 本番の採番戦略は実装の自由。
 * 実装は AI が別ファイルに書く。default 禁止。
 */
interface VisitIdGenerator {
	/** 次の同一性を配る(1 回の供給 — 供給ごとに前へ進む)。 */
	fun nextId(): VisitId
}

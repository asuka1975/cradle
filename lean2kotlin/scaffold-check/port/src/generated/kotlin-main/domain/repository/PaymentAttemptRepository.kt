// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.lobby.domain.repository

import dev.cradle.lobby.domain.entity.PaymentAttempt
import dev.cradle.lobby.domain.valueobject.PaymentAttemptId

/**
 * Lean: `Lobby.Domain.PaymentAttempt` 集約のリポジトリ(トランザクション境界の単位)。
 * 操作はドメインの遷移から許可されるものだけ(削除の遷移が無いため remove は無い)。
 * 実装は AI が別ファイルに書く。default 禁止。
 */
interface PaymentAttemptRepository {
	/** 宛先解決(コマンドが同一性を運ぶ根拠から導出)。 */
	fun findById(id: PaymentAttemptId): PaymentAttempt?
	/** 集約の集合の観測(Repository = 集約ルート集合)。並びは保存順。 */
	fun findAll(): List<PaymentAttempt>
	/** 新規個体の追加 = 末尾への追加(生成の遷移 create がある根拠から導出)。 */
	fun add(paymentAttempt: PaymentAttempt)
	/** 既存個体の書き換え(書き換えの遷移がある根拠から導出)。並びは保たれる。 */
	fun update(paymentAttempt: PaymentAttempt)
}

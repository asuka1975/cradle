// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.lobby.domain.entity

import dev.cradle.lobby.domain.valueobject.PaymentAttemptId
import dev.cradle.lobby.domain.valueobject.PaymentPhase
import dev.cradle.lobby.domain.valueobject.PaymentResult
import dev.cradle.lobby.domain.valueobject.VisitId

/**
 * Lean: `Lobby.Domain.PaymentAttempt`(aggregateRoot)
 */
interface PaymentAttempt {
	val id: PaymentAttemptId
	val visit: VisitId
	val amount: Long
	val tries: Long
	val phase: PaymentPhase
	/** その確定結果で決着しているか（同じ結果の通知の重複の判定）。 */
	fun settledAs(r: PaymentResult): Boolean
	/** 確定する。 */
	fun settle(r: PaymentResult): PaymentAttempt
	/** 受け付けられ、確定の通知を待つ。 */
	fun awaitConfirmation(): PaymentAttempt
	/** 提供元に届いていなかった: 送れる状態に戻す（同じ冪等キーで送り直す）。 */
	fun resetPending(): PaymentAttempt
	/** 確定しているか。 */
	fun isSettled(): Boolean
	/** 送ったが答えを失った（送り直さず照会する）。 */
	fun lose(): PaymentAttempt
	/** 送っている途中（印つき）か、答えを失ったか、通知待ちか — 照会で確かめる状態。 */
	fun isInquirable(): Boolean
	/** 送る前に印を付ける: 試行番号を進め、送ったかどうか分からない状態（sending）にする（冪等キーは変えない）。 */
	fun markSending(): PaymentAttempt
}

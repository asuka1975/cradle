// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.scaffold.application

import dev.cradle.scaffold.runtime.UserId

/**
 * Lean: `Sprout.Application.ActorContext`(actorPort — 操作の主体の調達ポート)。
 * いまその人自身として操作している利用者。
 *
 * 値の調達(認証)は境界の関心 — **この型を構築してよいのは認証アダプタだけ**
 * (リクエストのペイロードから組み立ててはならない: クライアントが主体を
 * 名乗れる形は認可の破れになる)。UseCase 実装はリクエストスコープの配線
 * (resolver 等)で受け取る — 本番の UseCase 署名には現れない。
 * 契約テストは固定の主体をフックで注入する — 主体依存のふるまい(認可分岐)は
 * そちらが検証する。**組み立ての正しさ(認証情報 → この型)は生成テストの
 * 保証の外** — 境界のテスト・構築箇所を縛る lint を別途持つこと。
 */
data class ActorContext(
	val user: UserId,
)

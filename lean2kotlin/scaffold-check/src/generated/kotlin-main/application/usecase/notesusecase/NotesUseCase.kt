// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.scaffold.application.usecase.notesusecase

import dev.cradle.scaffold.DomainResult
import dev.cradle.scaffold.application.NoteView
import dev.cradle.scaffold.application.QueryError

/**
 * Lean: モジュール `NotesUseCase` の interface 面(署名は Lean の def の写し)。
 * アプリケーション層の UseCase(固定形 validate / execute)。
 * 取得(読み取りストア)・乱択の鍵・調達ポート(時計・主体・外部能力の観測)・precise な入力
 * (lookup 結果・採番列)は**実装の配線**(注入ポート)— 署名には現れない
 * (証明装置を本番界面に写さない)。効果はリポジトリ経由で観測する。
 * 実装は AI が別ファイルに書く。default 禁止。
 */
interface NotesUseCase {
	/**
	 * 検証対象が無いので常に成功（型の形が表明）。
	 * 本番では execute の内部第一段として呼ばれる(実装は「execute は
	 * validate を呼ぶ」形に一本化する — 分岐条件を再実装しない)。
	 * 公開メンバとしては**テストシーム**(定理→テスト 1:1 導出・読み取り専用の検査)。
	 */
	fun validate(`_q`: NotesQuery): DomainResult<QueryError, Unit>
	fun execute(q: NotesQuery): DomainResult<QueryError, List<NoteView>>
}

// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.scaffold.domain.valueobject

import dev.cradle.scaffold.TitleFixture
import org.junit.jupiter.api.Assertions.assertEquals
import org.junit.jupiter.api.Test

/**
 * Lean: @[contract] 定理群から演繹された `Title` の単体テスト。
 * ふるまいは Entity interface のメソッド — 入力は実体化フック
 * (fixture → 実装の Entity)で作り、返り値は観測(toFixture)で比較する。
 */
abstract class TitleEntityContractTest {
	/** fixture の実体化(観測が一致する実装の Entity を返す)。 */
	protected abstract fun title(fixture: TitleFixture): Title

	/** 制約の特徴付け（境界値テストの生成源）。 */
	@Test
	fun `valid は定理 valid_iff を再現する(1)`() {
		assertEquals(true,
			title(TitleFixture(text = "s0")).valid())
	}

	/** 制約の特徴付け（境界値テストの生成源）。 */
	@Test
	fun `valid は定理 valid_iff を再現する(2)`() {
		assertEquals(true,
			title(TitleFixture(text = "s1")).valid())
	}

	/** 制約の特徴付け（境界値テストの生成源）。 */
	@Test
	fun `valid は定理 valid_iff を再現する(3)`() {
		assertEquals(true,
			title(TitleFixture(text = "s2")).valid())
	}

	/** 制約の特徴付け（境界値テストの生成源）。 */
	@Test
	fun `valid は定理 valid_iff を再現する(4)`() {
		assertEquals(true,
			title(TitleFixture(text = "s3")).valid())
	}
}

// GENERATED by lean2kotlin — DO NOT EDIT
// 再生成で全上書きされます。実装はこのファイルではなく別ファイルに書いてください。

package dev.cradle.scaffold

import dev.cradle.scaffold.domain.entity.Note
import dev.cradle.scaffold.domain.valueobject.NoteId
import dev.cradle.scaffold.domain.valueobject.Title
import dev.cradle.scaffold.runtime.UserId

// Entity / 集約ルートの fixture と観測正規化(toFixture)。
// 本番の Entity は interface(getter)— 等価性はクラスではなく観測で比較する。
// 橋渡し型(entity-like を運ぶ sealed / VO 構造)は平行 fixture を持つ —
// fixture 語彙の閉包(観測レコードは本番 interface を運ばない)。

data class NoteFixture(val id: NoteId, val author: UserId, val title: TitleFixture, val closed: Boolean)

data class TitleFixture(val text: String)

fun Note.toFixture(): NoteFixture = NoteFixture(id = id, author = author, title = title.toFixture(), closed = closed)

fun Title.toFixture(): TitleFixture = TitleFixture(text = text)
